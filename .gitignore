# user data & secrets
downloads/
clips/
# saved projects: transcripts, clip lists and thumbnails — in desktop mode at the
# root, in server mode one folder per user under users/
projects/
users/
config.json
*.mp4
*.mov

# deployment secrets & runtime data (the .env has passwords, data/ has videos)
deploy/.env
deploy/data/

# python
__pycache__/
*.pyc
.venv/

# build artifacts (CI regenerates these)
build/
build_obf/
dist/
release/
frontend/node_modules/
frontend/dist/

# obsolete streamlit era
.streamlit/

# keep the PyInstaller specs (build.spec, build_obf.spec) — they are source
